<%= form_for(@char_note) do |f| %>
  <% if @char_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@char_note.errors.count, "error") %> prohibited this char_note from being saved:</h2>

      <ul>
      <% @char_note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :character %><br>
    <%= collection_select(:char_note, :character_id, Character.all, :id, :name) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
